import React from "react";
import styled from "styled-components";

export const Splash: React.FC = () => {
  return <Title>dudel.</Title>;
};

const Title = styled.p`
  font-family: Helvetica;
  color: teal;
  font-size: 24px;
  font-weight: bold;
`;
